# 验收记录

## 新增 fufu：2026-09-08

此次更新仅合并新玩偶资源并调整对应检查，沿用现有 Three.js 加载和交互。

- 保留原房间 784 个网格，增加 fufu 的 63 个独立网格；原有两个初音玩偶仍在。
- 合并时逐项验证原网格定义、原节点属性及原始二进制数据保持不变；床分组增加一个玩偶子节点。
- 新增 3 张程序生成的布料法线贴图，内嵌贴图合计 31 张。原有 28 张校验值保持不变；参考照片、视频、SPZ 均留在本地。
- 新 GLB：25,545,080 字节；SHA-256：`2a175cb06605d6761c5cdb21b86356a2883e6720d8c6b02279444c36eab27367`。
- 独立 Blender 工程、GLB、扫描轮廓叠图及正面、侧面、背面、俯视和床上预览保存在本地交付目录。
- 轮廓以扫描为约束；五官和服装按照片、视频重建。隐藏的底部及部分褶皱为近似补全，短绒采用法线贴图，不等同于逐根绒毛或完全实物复刻。

生产预览地址：`http://127.0.0.1:4173/wangwei-room/`。本次实际复验：

| 检查项 | 结果 |
| --- | --- |
| 8 项自动化测试 | 全部通过，含床面接触、40 cm 尺寸、旧玩偶保留及材质清单 |
| TypeScript 与 Vite 生产构建 | 通过，构建中的模型与源模型 SHA-256 相同 |
| 桌面 1440 × 1000、手机 390 × 844 | 布局正常，无横向溢出，主要触摸按钮至少 44 px |
| 全景、书桌、重置、鼠标平移和缩放 | 正常 |
| 手机单指旋转、双指缩放 | 正常，未误触屏幕开关 |
| 房间灯光与屏幕独立开关 | 正常 |
| 床上近景 | 五官与衣服分明，无明显穿插；玩偶底部接触床面 |
| 页面、JS、CSS、模型请求 | 200，无脚本异常 |

控制台有现有 Three.js 的 `PCFSoftShadowMap` 弃用提示，浏览器自动采用 `PCFShadowMap`，不影响本次模型加载。手机使用 Chromium 视口与触摸模拟，尚未进行 iOS Safari 真机测试。

部署前 `gh-pages` 版本保留在 Git 历史：`d5fd386aee57961d868efe5eb98729144e2fa870`，可用于恢复上一版。

发布版本：`c59699dd9a6efe50de7e80c593680a7efb42a786`。GitHub Pages 状态为 `built`。2026-09-08 02:40（北京时间）从公开地址重新下载，页面与模型返回 200，模型大小及 SHA-256 与上述生产构建完全一致。随后在浏览器打开公开页面，确认新增 fufu 出现在床上，JS/CSS/GLB 均返回 200；书桌视角、重置、灯光和屏幕独立开关复验通过。

## 原房间发布验收（新增 fufu 之前）

日期：2026-09-08。浏览器：ego-lite（Chromium），使用真实鼠标、滚轮和 CDP 触摸输入验收生产构建。

## 照片清理

- 网页目录及构建产物没有 JPG、JPEG、HEIC、PNG、WebP 等独立照片或旧预览图文件。
- 从 GLB 移除 1 张真实照片、3 个纹理槽及对应二进制数据；重新打包所有保留的 buffer view。
- 剩余 28 张内嵌 PNG 与建模时程序生成的材质文件逐个校验一致，详见测试夹具中的 SHA-256。
- 屏幕和相框图案由运行时代码生成，不读取照片或远程图片。
- 清理后 GLB：20,370,764 字节。
- SHA-256：`4b0ce2163e1ec3fe62db5a8e0a22cd1575089e5c8c36cf23acf5abc5bb7f02d1`。
- 源码不依赖项目外的原始模型。私有扫描、Blender 工程及参考素材未纳入仓库。

## 本地生产构建

地址：`http://127.0.0.1:4173/wangwei-room/`。

| 检查项 | 结果 |
| --- | --- |
| TypeScript 与 Vite 生产构建 | 通过 |
| 7 项自动化测试 | 全部通过 |
| 页面、JS、CSS、模型、图标请求 | 全部 200 |
| 桌面 1440 × 900 | 布局正常，无横向溢出 |
| 手机 390 × 844 | 布局正常，主要按钮触摸高度至少 44 px |
| 全景、书桌、重置 | 正常切换 |
| 鼠标旋转、滚轮缩放、右键平移 | 画面发生预期变化 |
| 单指旋转、双指缩放和平移 | 画面发生预期变化，无屏幕误触 |
| 直接点击或触摸显示器 | 正常开关屏幕 |
| 从屏幕开始拖拽并返回起点 | 未误触开关 |
| 灯光与屏幕独立开关 | 状态和画面一致 |
| 关灯背景 | 保持页面底色，无整块灰色背景 |
| 加载进度 | 限速加载时观察到实际进度，控件暂时禁用 |
| 阻断模型请求后重试 | 显示错误提示，解除阻断后恢复；仅保留一个 Canvas |
| WebGL 不可用 | 显示图标和文字说明，未显示旧照片预览 |
| 恢复正常环境 | 页面正常，无意外脚本错误或资源失败 |

正常访问中的控制台和网络检查未发现错误。故障注入时预期出现的网络或图形错误单独验证，不计为正常访问错误。

手机验收使用 Chromium 的视口及触摸模拟，不等同于 iOS Safari 真机测试。
